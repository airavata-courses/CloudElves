version: 2.1

jobs:
  build:
    working_directory: ~/registry
    docker:
      - image: "cimg/openjdk:11.0.13"
    steps:
      - checkout
      - run: ls -lrt
      - run: mvn package -Dmaven.test.skip -f registry/pom.xml
