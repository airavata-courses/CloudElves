create table UserDetails(user_id text, user_email text, register_timestamp timestamp, active boolean, primary key(user_id));
create table AppLog(id text, user_id text, service_id text, log_timestamp timestamp, status integer, comments text, primary key(id), foreign key(user_id) references userdetails(user_id));